box: python:3.4-wheezy
services:
   - id: mongo
build:
  steps:
    - pip-install:
        requirements_file: "requirements.txt"
    - script:
        name: unit test
        code: tox
    - script:
        name: echo python information
        code: |
          echo "python version $(python --version) running"
          echo "pip version $(pip --version) running"
          pip freeze
